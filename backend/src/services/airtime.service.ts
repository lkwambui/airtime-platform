export async function sendAirtime(phone: string, amount: number) {
  // SAFARICOM AIRTIME API WILL GO HERE
  // Stubbed for now
  console.log(`📞 Sending airtime ${amount} to ${phone}`);
  return true;
}
